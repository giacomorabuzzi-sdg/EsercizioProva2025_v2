sources:
  - name: negozio
    database: prova2
    schema: models
    tables:

      - name: products_t0
        description: Tabella dei prodotti con i dati storici iniziali (versione 0).
        tests:
          - not_null:
              column_name: product_cd

      - name: customers_t0
        description: Tabella dei clienti con i dati storici iniziali (versione 0).
        tests:
          - not_null:
              column_name: customers_cd

      - name: products_t1
        description: Tabella dei prodotti con i dati più recenti (versione 1).
        tests:
          - not_null:
              column_name: product_cd

      - name: customers_t1
        description: Tabella dei clienti con i dati più recenti (versione 1).
        tests:
          - not_null:
              column_name: customers_cd